const chunking = (data = []) => {
    const sentence = "i am dixit ahir kill me into pices";

    for (let index = 0; index < sentence.length; index++) {
        const element = sentence[index];
        console.log(element);
    };

    // return data;
};


module.exports = chunking;